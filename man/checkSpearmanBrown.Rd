% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkSpearmanbrown.R
\name{checkSpearmanBrown}
\alias{checkSpearmanBrown}
\title{Compute Spearman-Brown Reliability Coefficient}
\usage{
checkSpearmanBrown(half1, half2, B = 500, conf = 0.95, na.rm = TRUE)
}
\arguments{
\item{half1}{A numeric matrix or data frame containing item scores for the first test half.}

\item{half2}{A numeric matrix or data frame containing item scores for the second test half.}

\item{B}{Integer. Number of bootstrap resamples (default = 500).}

\item{conf}{Numeric. Confidence level (default = 0.95).}

\item{na.rm}{Logical. If TRUE (default), rows with any missing values in either half are removed.}
}
\value{
A data frame with columns:
\code{Coefficient} ("Spearman-Brown"),
\code{Estimate} (estimated reliability),
\code{lwr.ci}, \code{upr.ci} (bootstrap confidence interval).
}
\description{
Calculates the Spearman-Brown reliability coefficient for two test halves
and estimates confidence intervals using bootstrap resampling.
}
\details{
The Spearman-Brown coefficient is computed as:
\deqn{r_{SB} = \frac{2 r}{1 + r}}
where \eqn{r} is the Pearson correlation between the total scores of the two halves.

Confidence intervals are estimated via percentile bootstrap.
}
\examples{
set.seed(123)
half1 <- matrix(rnorm(100*5), ncol=5)
half2 <- matrix(rnorm(100*5), ncol=5)
checkSpearmanBrown(half1, half2)

}
