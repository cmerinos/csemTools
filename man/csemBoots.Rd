% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csemBoots.R
\name{csemBoots}
\alias{csemBoots}
\title{Bootstrap Conditional Standard Error of Measurement (CSEM) based on Item Sampling}
\usage{
csemBoots(
  data,
  B = 500,
  cores = 1,
  bin.score = NULL,
  smooth = FALSE,
  degree = 2,
  full.range = FALSE,
  ci = FALSE,
  conf.level = 0.95,
  digits = 3,
  score.range = NULL,
  na.rm = TRUE
)
}
\arguments{
\item{data}{A data frame or matrix containing the item responses.
Rows represent persons, columns represent items. Items can be
dichotomous or polytomous (e.g., Likert scales).}

\item{B}{Integer. Number of bootstrap replications (default = 500).}

\item{cores}{Integer. Number of CPU cores to use for parallel processing.
If \code{cores = 1} (default), the bootstrap runs sequentially with a progress bar.
If \code{cores > 1}, a parallel cluster is created and the bootstrap is distributed
across the cores. A progress bar is still shown (using \code{pbapply}).}

\item{bin.score}{Integer or NULL. If an integer (e.g., 5), persons are grouped
into that many quantile groups based on their total score, and the average
CSEM within each group is reported. If \code{NULL} (default), CSEM is reported
for each distinct observed total score (with at least 2 persons).}

\item{smooth}{Logical. If \code{TRUE}, a polynomial regression of the squared CSEM
on total score is fitted, and the smoothed CSEM (square root of the fitted
values) is reported. This stabilizes estimates at score levels with few
observations. Default = \code{FALSE}.}

\item{degree}{Integer. Polynomial degree used when \code{smooth = TRUE}. Default = 2.}

\item{full.range}{Logical. If \code{TRUE} and \code{smooth = TRUE}, the smoothed CSEM
is evaluated for every integer score in \code{score.range}. If \code{smooth = FALSE}
and \code{full.range = TRUE}, missing values are filled by carrying forward the
last observed CSEM (\code{na.locf}). Requires \code{score.range} to be provided.
Default = \code{FALSE}.}

\item{ci}{Logical. If \code{TRUE}, confidence intervals for the true score are
computed as \verb{score +/- z * CSEM}, assuming normally distributed errors.
The intervals are truncated to \code{score.range} (if supplied) or to the
observed range. Default = \code{FALSE}.}

\item{conf.level}{Numeric. Confidence level for the intervals (default = 0.95).}

\item{digits}{Integer. Number of decimal places for rounding the output.
Default = 3.}

\item{score.range}{Numeric vector of length 2 (min, max). Required when
\code{full.range = TRUE}. Defines the theoretical score range (e.g., c(10,50)).
Also used to truncate confidence intervals (if \code{ci = TRUE}). If \code{NULL},
the observed minimum and maximum are used for truncation.}

\item{na.rm}{Logical. If \code{TRUE} (default), rows (persons) with any missing
item responses are removed before analysis.}
}
\value{
A list with up to two elements:
\describe{
\item{CSEM}{A data frame with columns:
\itemize{
\item \code{score} : total score (integer).
\item \code{n} : number of persons with that score.
\item \code{CSEM} (or \code{CSEM.smooth}) : conditional standard error of measurement.
\item \code{lwr.ci}, \code{upr.ci} : confidence limits (if \code{ci = TRUE}).
}
}
\item{binned.CSEM}{If \code{bin.score} is provided, a data frame with:
\itemize{
\item \code{group} : quantile group number.
\item \code{range} : score range of the group.
\item \code{n} : number of persons in the group.
\item \code{mean_score} : average total score in the group.
\item \code{CSEM.mean} : average CSEM within the group.
\item \code{lwr.ci}, \code{upr.ci} : confidence limits (if \code{ci = TRUE}).
}
}
}
}
\description{
Estimates the conditional standard error of measurement for each total score
using a non parametric bootstrap of items (tasks). This method simulates
parallel test forms by resampling items with replacement and computes the
standard deviation of the resulting bootstrap scores for each person.
The individual standard errors are then averaged within each observed total
score to obtain the conditional SEM. The procedure follows the logic of
Colton, Gao, & Kolen (1996) but is adapted to continuous total scores
(sum of Likert items) rather than discrete performance levels.
}
\details{
\subsection{Methodological flow (adapted from Colton et al., 1996)}{
\enumerate{
\item The observed total score is computed for each person as the sum of item responses.
\item For each bootstrap replication \verb{b = 1 ... B}:
\itemize{
\item A bootstrap sample of \strong{items} (columns) is drawn with replacement,
preserving the original number of items.
\item For every person, the total score on the bootstrap item set is computed.
}
\item After all replications, each person has \code{B} bootstrap total scores.
The standard deviation of these \code{B} scores is the \strong{individual standard error
of measurement} (\eqn{\hat{\sigma}(\Delta_p)}).
\item Persons are grouped by their \strong{observed total score} (from the original data).
Within each score level, the individual error variances are averaged, and the
square root is taken: \eqn{CSEM(x) = \sqrt{ \frac{1}{n_x} \sum_{p: X_p=x} \hat{\sigma}^2(\Delta_p) }}.
\item Optionally, a polynomial smoothing is applied to the squared CSEM values to
reduce sampling variability at score levels with few persons.
\item If \code{bin.score} is provided, the persons are divided into quantile groups
(based on the original total scores) and the average CSEM inside each group
is reported.
}
}

\subsection{Why bootstrap items (not persons)?}{
The standard error of measurement refers to the variability of a person's score
across hypothetically parallel test forms. Resampling persons would simulate
sampling from the population, not parallel forms. Resampling \strong{items} creates
different versions of the test, which is the correct approach to estimate the
conditional error variance. This is the essence of the method used by
Colton et al. (1996) for performance level scores, here extended to continuous
total scores.
}

\subsection{Parallel processing and progress bar}{
When \code{cores = 1}, the bootstrap runs sequentially and a progress bar is shown
via \code{pbapply::pblapply}. When \code{cores > 1}, a cluster is created using
\code{parallel::makeCluster}, the bootstrap is distributed with \code{pbapply::pblapply}
(which automatically displays a progress bar for parallel tasks), and the
cluster is stopped afterwards. The number of cores should not exceed the
available CPU cores.
}

\subsection{Handling of missing data and small groups}{
Persons with any missing item responses are removed if \code{na.rm = TRUE}.
Score levels with fewer than 2 persons receive a missing CSEM (NA) in the raw
output; they are omitted from smoothing and from the \code{full.range} expansion
(except when \code{full.range = TRUE} and \code{smooth = FALSE}, where NAs are filled
by last observation carried forward).
}
}
\examples{
\donttest{
# Simulate data: 500 persons, 10 Likert items (1-5)
set.seed(123)
resp <- matrix(sample(1:5, 500*10, replace = TRUE), nrow = 500)

# Basic usage (sequential, no smoothing)
result <- csemBoots(resp, B = 100, cores = 1)
head(result$CSEM)

# With smoothing, full range, and confidence intervals
result2 <- csemBoots(resp, B = 200, smooth = TRUE, full.range = TRUE,
                     score.range = c(10, 50), ci = TRUE, cores = 2)
print(result2$CSEM)

# Quantile groups (quintiles)
result3 <- csemBoots(resp, B = 100, bin.score = 5)
print(result3$binned.CSEM)
}

}
\references{
Colton, D. A., Gao, X., & Kolen, M. J. (1997). Assessing the reliability of
performance level scores using bootstrapping. ACT Research Report Series,
97-3. Iowa City, 1A: ACT.
}
