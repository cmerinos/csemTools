% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkSplit.R
\name{checkSplit}
\alias{checkSplit}
\title{Split a Test into Two Halves}
\usage{
checkSplit(
  data,
  method = c("ritc", "difficulty", "random", "optimized"),
  equal.size = TRUE,
  n_iter = 500,
  top_n = 10,
  seed = 123,
  na.rm = TRUE
)
}
\arguments{
\item{data}{A data frame or matrix of item responses (rows = persons, columns = items).}

\item{method}{Character: "ritc", "difficulty", "random", or "optimized".}

\item{equal.size}{Logical. If TRUE, forces halves to have the same number of items.}

\item{n_iter}{Integer. Number of random splits when method = "optimized".}

\item{top_n}{Integer. Number of top splits to report in summary when method = "optimized".}

\item{seed}{Integer for reproducibility.}

\item{na.rm}{Logical. If TRUE, removes rows with missing values.}
}
\value{
A list with components: half1, half2 (vectors of column names), summary (data frame),
and method.
}
\description{
Divides the items of a test into two halves using different methods.
}
\examples{
\donttest{
## Load data
library(EFA.dimensions)
data("data_RSE")

## Recode negative items
data_RSE[c("Q3", "Q5", "Q8", "Q9", "Q10")] <- 5 - data_RSE[c("Q3", "Q5", "Q8", "Q9", "Q10")]

## Check split
checkSplit(data = data_RSE, method = "ritc")
checkSplit(data = data_RSE, method = "random")
checkSplit(data = data_RSE, method = "optimized")
checkSplit(data = data_RSE, method = "difficulty")

## Choosing split by difficulty criteria
RSE.namesHalf <- checkSplit(data = data_RSE, method = "difficulty")

RSE.namesHalf$half1
RSE.namesHalf$half2
}

}
