% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csemFSG.R
\name{csemFSG}
\alias{csemFSG}
\title{ANOVA method for Conditional Standard Error of Measurement (CSEM)}
\usage{
csemFSG(
  data,
  bin.score = NULL,
  smooth = FALSE,
  degree = 2,
  full.range = FALSE,
  ci = FALSE,
  conf.level = 0.95,
  digits = 3,
  score.range = NULL,
  na.rm = TRUE
)
}
\arguments{
\item{data}{A data frame or matrix with item responses (subjects in rows,
items in columns). Items can be dichotomous or polytomous.}

\item{bin.score}{integer. Number of quantile groups (e.g., 5 for quintiles).
If \code{NULL} (default), CSEM is reported for each observed score (with \code{n >= 2}).}

\item{smooth}{logical. If \code{TRUE}, applies polynomial smoothing to the squared
CSEM estimates. Default = \code{FALSE}.}

\item{degree}{integer. Polynomial degree used when \code{smooth = TRUE}. Default = 2.}

\item{full.range}{logical. If \code{TRUE} and \code{smooth = TRUE}, evaluates the smoothed
CSEM for every integer score from \code{score.range[1]} to \code{score.range[2]}.
Requires \code{score.range}. Default = \code{FALSE}.}

\item{ci}{logical. If \code{TRUE}, compute confidence intervals for the true score.
Default = \code{FALSE}.}

\item{conf.level}{numeric. Confidence level for intervals (default 0.95).}

\item{digits}{integer. Rounding for CSEM and confidence limits. Default = 3.}

\item{score.range}{numeric vector of length 2 (min, max). Required when
\code{full.range = TRUE}. Defines the theoretical score range (e.g., c(0,36)).
Also used to truncate confidence intervals (if ci=TRUE). If \code{NULL}, the
observed range is used for truncation.}

\item{na.rm}{logical. If \code{TRUE} (default), removes rows with any missing values.}
}
\value{
A list with elements:
\item{CSEM}{data.frame with columns \code{score}, \code{n},
and either \code{CSEM} (raw) or \code{CSEM.smooth} (smoothed). If \code{ci = TRUE}, also
\code{lwr.ci} and \code{upr.ci} (truncated to possible score range).}
\item{binned.CSEM}{(if \code{bin.score} is integer) data.frame with quantile groups:
\code{group}, \code{range}, \code{n}, \code{mean_score}, \code{CSEM.mean}, and intervals if \code{ci = TRUE}.}
}
\description{
Estimates conditional standard errors of measurement using the variance-components
approach described in Feldt, Steffen, & Gupta (1985). This is the method
referred to as "ANOVA" in the JASP Reliability module.

For each unique total score (or quantile group), the CSEM is computed as:
\deqn{CSEM = \sqrt{ \frac{J}{J-1} \sum_{j=1}^J s_j^2 }}
where \eqn{J} is the number of items and \eqn{s_j^2} is the sample variance
of item \eqn{j} within the group (using divisor \eqn{n_g - 1}).

The function offers polynomial smoothing (\code{smooth = TRUE}) and expansion to
a full range of integer scores (\code{full.range = TRUE}). It also allows
aggregation into quantile groups (\code{bin.score}).
}
\references{
Feldt, L. S., Steffen, M., & Gupta, N. C. (1985).
A comparison of five methods for estimating the standard error of measurement
at specific score levels. \emph{Applied Psychological Measurement}, 9(4), 351–361.
}
