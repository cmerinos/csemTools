% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotCSEM.R
\name{plotCSEM}
\alias{plotCSEM}
\title{Plot Conditional Standard Error of Measurement (CSEM) Curve}
\usage{
plotCSEM(
  data,
  x = "Score",
  y = "CSEM",
  plot.type = c("CSEM", "CI", "band"),
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  color.line = "black",
  color.points = "darkred",
  color.band = "lightblue",
  line.type = "solid",
  point.size = 2,
  save.path = NULL,
  width = 8,
  height = 6
)
}
\arguments{
\item{data}{A data frame containing at least columns \code{Score} and \code{CSEM}.
Optionally \code{lwr.ci} and \code{upr.ci} for confidence intervals.}

\item{x}{Character. Name of the column to use on x-axis (default = "Score").}

\item{y}{Character. Name of the column to use on y-axis for CSEM (default = "CSEM").}

\item{plot.type}{Character: \code{"CSEM"} (only the CSEM line/points),
\code{"CI"} (CSEM with error bars/ribbon), or \code{"band"} (width of confidence band).}

\item{title}{Character. Plot title. If NULL, a default title is generated.}

\item{xlab}{Character. X-axis label. If NULL, uses \code{x}.}

\item{ylab}{Character. Y-axis label. If NULL, auto-generated.}

\item{color.line}{Color for the line (default = "black").}

\item{color.points}{Color for points (default = "darkred").}

\item{color.band}{Color for confidence band/ribbon (default = "lightblue").}

\item{line.type}{Line type (default = "solid").}

\item{point.size}{Size of points (default = 2).}

\item{save.path}{Optional file path to save plot as PNG. If NULL, plot is not saved.}

\item{width}{Numeric. Width of saved plot in inches (default = 8).}

\item{height}{Numeric. Height of saved plot in inches (default = 6).}
}
\value{
A ggplot2 object (invisibly). The plot is also printed.
}
\description{
Creates a ggplot2 plot of the CSEM as a function of test scores, optionally
including confidence intervals or confidence band widths. Can be used with
any CSEM function that returns a data frame with columns \code{Score}, \code{CSEM}, and
optionally \code{lwr.ci}, \code{upr.ci}.
}
\examples{
\donttest{
## Load data
library(EFA.dimensions)
data("data_RSE")

## Recode negative items
data_RSE[c("Q3", "Q5", "Q8", "Q9", "Q10")] <- 5 - data_RSE[c("Q3", "Q5", "Q8", "Q9", "Q10")]

## Split in two halves
RSE.namesHalf <- checkSplit(data = data_RSE, method = "difficulty")

## Items in the halves?
RSE.namesHalf$half1
RSE.namesHalf$half2

# Mollenkopst-Feldt method
mfres <- csemMF(data_RSE[, RSE.namesHalf$half1,]
data_RSE[RSE.namesHalf$half1],
degree = 2, ci = TRUE)

plotCSEM(data = mfres$CSEM,
x = "score",
y = "CSEM.smooth", plot.type = "CSEM")
}
}
